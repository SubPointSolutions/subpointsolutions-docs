step 1, setup your definition

step 2, setup your site models
 taxonomyModel = SPMeta2Model.NewSiteModel(site =>
            {
 // setup site taxonomy
            });

 featuresAndSandboxSolutionsModel = SPMeta2Model.NewSiteModel(site =>
            {
 // setup sandbox solutions and features
            });

 fieldsAndContentTypesModel = SPMeta2Model.NewSiteModel(site =>
            {
 // setup fields and content types
            });

step 3, deploy site models

