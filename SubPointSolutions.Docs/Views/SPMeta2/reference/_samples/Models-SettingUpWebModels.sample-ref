step 1, setup your definition

step 2, setup your site models
 featuresModel = SPMeta2Model.NewWebModel(web =>
            {
 // setup features
            });

 listsModel = SPMeta2Model.NewWebModel(web =>
            {
 // setup fields and content types
            });

 navigationModel = SPMeta2Model.NewWebModel(web =>
            {
 // setup web navigation
            });

step 3, deploy web models

