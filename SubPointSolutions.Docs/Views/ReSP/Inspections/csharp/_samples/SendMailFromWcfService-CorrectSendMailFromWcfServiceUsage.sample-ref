save current context
pContext curCtx = HttpContext.Current;
pContext.Current = null; // <-- recommended approach for wcf services

l success = SPUtility.SendEmail(web, true, true, to, subject, body);

restore the context
pContext.Current = curCtx;

